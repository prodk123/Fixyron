# FIXYRON Tools

## Overview
This directory is reserved for the tools that FIXYRON AI agents will use to interact with the environment.

## Future Tools
- Repository inspection tools (read files, list directories, search code).
- Execution tools (run tests, run build scripts).
- Git tools (create branches, commit, push, create pull requests).
